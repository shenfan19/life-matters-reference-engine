# 软件设计
## 输入数据的数学结构
### Regimen的K×4参数空间

一个Regimen有且仅有四个维度，每个维度可独立设置为**锁定**或**优化**：

| 维度 | 含义 | 锁定时 | 优化时 |
|------|------|--------|--------|
| `time` | 一天内的时间点序列（几点做） | 固定时刻 | 搜索范围 [earliest, latest] |
| `value` | 每个时间点的强度值（做多少） | 固定数值 | 搜索范围 [min, max] |
| `days` | 哪几天执行 | 固定天集合 | 勾选哪些天可变 |
| `valid_range` | 起止日期（可关闭） | 固定区间或关闭 | 搜索范围或关闭 |

**当valid_range关闭时**：退化为无起止限制的日常Regimen，等价于整个仿真期内一直有效。

**优化搜索空间维度计算**：

$$d = \sum_{r=1}^{K} \left( n_r^{time} \cdot \mathbb{1}[time_r \text{ 优化}] + n_r^{value} \cdot \mathbb{1}[value_r \text{ 优化}] + 7 \cdot \mathbb{1}[days_r \text{ 优化}] + 2 \cdot \mathbb{1}[range_r \text{ 优化且开启}] \right)$$

其中 $n_r^{time}$ 和 $n_r^{value}$ 是第 $r$ 个Regimen的时间点数量。

### 概率事件参数（随机场景的处理）

随机事件（战死、手术风险、疾病发作）不通过蒙特卡洛模拟，而是以**概率常数**纳入模型动力学，以期望值轨迹运行仿真：

```yaml
# 概率事件参数，不进入优化搜索空间，只影响模型动力学
probability_params:
  combat_death_rate:
    description: "参战时日死亡概率"
    value: 0.008          # 索姆河战役：约0.8%/天
    unit: "prob/day"
    reference: "Prior 1992, Historical Journal"
    type: probability_constant  # 区别于普通参数

  surgery_mortality:
    description: "手术死亡概率（单次事件）"
    value: 0.03           # 3%，来自文献
    unit: "prob/event"
    reference: "相关外科文献"
    type: probability_constant

  disease_incidence:
    description: "某病年发病率"
    value: 0.05           # 5%/年
    unit: "prob/year"
    type: probability_constant
```

**在仿真中的处理方式**：

```
生存率轨迹 = ∏(1 - death_rate_per_step × step_size)
```

不需要随机采样，直接得到期望存活率的确定性轨迹。这比蒙特卡洛更快，且结果可重现，足够用于Pareto优化。

### iCal双向转换工具

iCal对应关系详见 c_matter_task_3 §1.2。**工具价值**：用户可以在手机日历App里直接设计自己的行为计划，导出iCal后一键导入LM仿真。这是一个独立的易用性贡献，适合放在Paper 1的附录或作为开源工具。

## Pareto输出的形态

### Pareto前沿是什么
对于两目标优化，输出是一条**权衡曲线**（50-200个非支配解），每个点代表一种权衡下的最优Regimen方案：
```
目标1：肝脂肪减少量（越大越好）
                ↑
              * |
           *    |         每个*是一个具体可执行的Regimen方案
        *       |
     *          |
  *             |
                +——————————————→
                  目标2：ALT酶峰值（越小越好）

左上角 = 激进运动方案（最大减脂，但ALT风险高）
右下角 = 保守休息方案（ALT安全，但减脂少）
中间弯折点 = 推荐的最佳权衡方案
```

### 用户如何使用Pareto输出
1. 界面显示Pareto散点图
2. 用户点击某个点 → 右侧展示该方案对应的具体Regimen时间表
3. 用户可拖动"偏好滑条"（偏向哪个目标更重要）→ 系统高亮推荐点
4. 用户导出选中的Regimen → 可导出为iCal格式（直接导入手机日历）

## 界面规划
### 核心设计理念

**Opt内嵌于Sim界面**：不是两个独立Tab，而是在Sim界面顶部有一个"优化模式"开关。开启后，输入区域的常数输入框变为范围输入框（可锁定或优化），其余布局不变。

### 整体布局（三列式）
```
┌─────────────────────────────────────────────────────────────────┐
│  [Story选择器]  [仿真时长]  [步长]    [ 🔀 优化模式 OFF/ON ]    │
├─────────────────┬───────────────────────┬───────────────────────┤
│                 │                       │                       │
│  左列：输入区   │  中列：仿真曲线区     │  右列：元数据区       │
│                 │                       │                       │
│  Variables      │  实时/结果曲线图      │  模型引用文献         │
│  Parameters     │  （多变量叠加）       │  参数来源             │
│  Regimens       │                       │  约束状态显示         │
│  Prob.Constants │                       │  （绿/红指示）        │
│                 │                       │                       │
├─────────────────┴───────────────────────┴───────────────────────┤
│  [▶ 运行仿真]  [⏸ 暂停]  [⏹ 停止]     进度条 ████████░░ 80%   │
└─────────────────────────────────────────────────────────────────┘
```

### 左列：输入区详细设计

#### 普通仿真模式（Opt关闭）

```
┌─── VARIABLES ────────────────────┐
│ liver_fat_percentage              │
│  初始值: [15.0] %                 │
│                                   │
│ exercise_intensity                │
│  当前值: [0.6]                    │
└───────────────────────────────────┘

┌─── REGIMENS ─────────────────────┐
│ exercise_plan                     │
│  recurrence: [weekly ▼]           │
│  days: ☑Mon ☐Tue ☑Wed ☐Thu       │
│         ☑Fri ☐Sat ☐Sun            │
│  时间点:                           │
│    07:00 → [0.6]                  │
│    08:00 → [0.0]                  │
│  有效期: [关闭 ▼]                 │
└───────────────────────────────────┘

┌─── PROBABILITY CONSTANTS ────────┐
│ 死亡概率类（不可优化，仅展示）    │
│  combat_death_rate: 0.008/day     │
│  surgery_mortality: 0.03/event   │
└───────────────────────────────────┘
```

#### 优化模式（Opt开启后，输入区变形）

```
┌─── REGIMENS（优化模式）──────────┐
│ exercise_plan                     │
│                                   │
│  days: ☑Mon ☑Tue ☑Wed ☑Thu       │  ← 可勾选，勾选=纳入优化
│        ☑Fri ☐Sat ☐Sun            │
│  [🔒 锁定天数] / [🔀 优化天数]   │  ← 整组切换
│                                   │
│  时间点1: 07:00                   │
│    time:  [🔒 07:00]              │  ← 🔒=锁定，显示固定值
│    value: [🔀 0.0 ~ 1.0]         │  ← 🔀=优化，显示搜索范围
│                                   │
│  时间点2: 08:00                   │
│    time:  [🔀 06:00 ~ 10:00]     │
│    value: [🔒 0.0]               │
│                                   │
│  有效期: [关闭 ▼]                 │
└───────────────────────────────────┘

┌─── OBJECTIVES（优化目标）────────┐
│  + 添加目标                       │
│                                   │
│  ① liver_fat_percentage           │
│     方向: [最小化 ▼]              │
│     [×删除]                       │
│                                   │
│  ② alt_enzyme_level               │
│     方向: [最小化 ▼]              │
│     [×删除]                       │
└───────────────────────────────────┘

┌─── CONSTRAINTS（约束）───────────┐
│  + 添加约束                       │
│  alt_enzyme_level ≤ [120] U/L    │
│  weekly_exercise ≥ [60] min      │
└───────────────────────────────────┘

┌─── 算法配置 ──────────────────────┐
│  算法: [NSGA-II ▼]                │
│  种群: [100]  代数: [200]         │
└───────────────────────────────────┘
```

### 中列：曲线区详细设计

#### 普通仿真模式

```
┌─── 仿真曲线 ──────────────────────────────────┐
│  [liver_fat%] [alt_level] [glucose] + 添加     │  ← 变量选择
│                                                 │
│  100%│                                          │
│      │  ╲                                       │
│   50%│    ╲___                                 │
│      │        ╲___________                     │
│    0%└──────────────────────→ 时间(天)          │
│        0      30      60      90               │
└─────────────────────────────────────────────────┘
```

#### 优化模式（优化运行中）

```
┌─── 优化进度 ────────────────────────────────────┐
│  Generation 45/200  ████████░░░░░░░  种群收敛中  │
└─────────────────────────────────────────────────┘

┌─── Pareto前沿（实时更新）─────────────────────────┐
│  目标1: liver_fat减少量（↑更好）                  │
│   ↑                                              │
│   │        *  *                                  │
│   │      *                                       │
│   │    *                                         │
│   │  *                                           │
│   └────────────────────→ 目标2: ALT峰值（←更好）  │
│                                                  │
│  [点击某个点查看该Regimen详情]                    │
└───────────────────────────────────────────────────┘

┌─── 选中方案的Regimen预览 ─────────────────────────┐
│  当前选中：方案 #37                               │
│  exercise: 周一三五  07:00-08:00  强度0.72        │
│  预测结果: 肝脂↓42%  ALT峰值98 U/L               │
│  [导出为iCal] [设为当前仿真参数]                  │
└───────────────────────────────────────────────────┘
```

### 右列：元数据区

```
┌─── 模型信息 ──────────────────────┐
│ fat_metabolism v1.2               │
│ 📄 Donnelly et al. 2009          │
│ 📄 Promrat et al. 2010           │
│                                   │
│ glucose_metabolism v0.8           │
│ 📄 Bergman 1981 (Minimal Model)  │
└───────────────────────────────────┘

┌─── 约束状态 ──────────────────────┐
│ ✅ alt_enzyme_level < 120         │
│ ✅ weekly_exercise ≥ 60 min       │
│ ⚠️  muscle_mass 接近下限         │
└───────────────────────────────────┘

┌─── 参数置信度 ────────────────────┐
│ exercise_effect  ★★★★☆ 中高       │
│ fat_accumulation ★★★☆☆ 中等       │
│ alt_response     ★★☆☆☆ 较低       │
└───────────────────────────────────┘
```
