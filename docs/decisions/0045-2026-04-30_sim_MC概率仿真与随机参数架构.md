# ADR 0045 — 概率仿真与 Monte Carlo 架构：parameter 分布表达式、多 run 引擎、前端渲染
**日期**：2026-04-30（决策时间 2026-04-24，此时补录）
**状态**：已实施

---

## 背景

模型中存在个体差异（如 `cognitive_efficiency`、`plague_susceptibility`、`insulin_sensitivity`），需要一种机制表达参数的不确定性，并让仿真在多条轨迹间可见这种不确定性。

---

## 决策

### 决策一：`parameter` 变量支持分布表达式

`type: parameter` 的 `value` 字段可写分布字符串：

| 语法 | 含义 |
|------|------|
| `normal(μ, σ)` | 正态分布 |
| `uniform(a, b)` | 均匀分布 |
| `lognormal(μ, σ)` | 对数正态分布 |

- **确定性模式**（MC=1）：取均值，等价于 `value: μ`
- **MC 模式**（MC>1）：每条 run 用独立 `np.random.default_rng(seed)` 采样一次，整条 run 使用该采样值

```yaml
insulin_sensitivity:
  value: "normal(1.0, 0.25)"
  type: parameter
  bounds: [0.2, 2.5]
```

### 决策二：Sim 模式 —— 默认 N=1，可选 Monte Carlo

| 参数 | 默认 | 范围 |
|------|------|------|
| `sim_runs` | 1 | 1–100 |

- N=1：单条轨迹，快速预览
- N>1：N 条独立采样轨迹，前端展示不确定性范围

### 决策三：前端图表渲染

- N=1：单条实线
- N>1：N 条半透明细线（opacity ≈ 0.25）+ 一条均值粗线（opacity = 1.0）

### 决策四：Opt 模式 —— 方案 B（多条期望迭代）

优化器每次评估一组参数时运行 N_inner 条仿真，取**均值**作为目标函数值，避免单条固定种子的泛化性问题。

| 参数 | 默认 | 范围 |
|------|------|------|
| `opt_inner_runs` | 5 | 1–20 |
| `opt_verify_runs` | 20 | 5–50 |
| `opt_aggregation` | `mean` | `mean/min/median` |

后续演进方案 C：Phase 1 快速探索（N=1）→ Phase 2 精细收敛（N=5–10）→ 最终验证（N=20）。

### 决策五：随机种子管理

- Session 启动时预生成 `[seed_1, ..., seed_N]`，保证同一 session 内 N 条曲线可复现
- `session_seed` 从 start API 返回，写入前端 state 并在工具栏 Tooltip 中显示
- **用户可设定固定 seed**：
  - YAML `optimizer.mc.seed`（整数）→ 前端读取为 `mcSeed` → 随 `POST /api/simulation/start` 的 `seed` 字段传入
  - `mcSeed = null`（省略 YAML 字段）→ 后端每次随机生成 `session_seed`
  - `mcSeed = 整数` → 后端直接使用该值，相同 seed + 相同模型 = 完全相同的 N 条轨迹
- Opt 引擎读取 `optimizer.mc.seed`（优先于旧的 `algorithm.seed` 回退路径）
- 前端工具栏：MC× 数量输入框旁附 seed 输入框（`placeholder="rand"`，空=随机）

### 决策六：MC 分叉可见性原则

MC 分叉**只出现在被 MC 参数直接或间接影响的变量**。建模时需确保：
- 有 `normal(...)` 的 parameter 必须出现在至少一个公式的表达式中
- 观察分叉时应选择该 parameter 参与计算的 output 变量（而非 schedule 驱动的确定性变量）

---

## 结果

```
sim_engine/src/simulator_engine.py
  start_session()     支持 sim_runs + seed（可选），预生成种子列表，顺序执行 N 条 run
  batch_steps()       返回 N 组轨迹数据 + 均值组
  _collect_param_distributions()  从 _param_dist_raw 读取分布映射
  _apply_parameter_sampling()     per-run 独立采样写入变量值和 asteval symtable
  _multi_eval_objective()         Opt 内环 N_inner 条均值评估

sim_engine/src/api_server.py
  SimulationStartRequest 新增 seed: Optional[int] = None 字段

sim_engine/src/optimizer_engine.py
  mc_seed 改从 optimizer.mc.seed 读取（回退 algorithm.seed，最终默认 42）

sim_engine/src/model_structure/loader.py
  _apply_model_data() 检测 value: "normal(...)" → 存入 _param_dist_raw，var.value 替换为均值

sim_gui/src/types.ts
  SimulationState 新增 mcSeed: number | null

sim_gui/src/components/Simulator.tsx
  工具栏 MC× 旁新增 seed 输入框（空=随机，整数=固定可复现）
  读 YAML optimizer.mc.seed → 初始化 mcSeed
  API 调用时附带 seed 字段
  Plot 渲染：N=1 单线；N>1 半透明细线 + 均值粗线

docs/model.md
  optimizer.mc schema 新增 seed 字段注释

models/papers/**/*.yaml
  所有 paper 模型的 mc 块均加入 seed: 42（确保论文结果可复现）
```
