# Sim-Game Merge 架构评估报告

## 1. 核心架构与定位评估

### 符合度：**95% (极高)**
您的 "Merge" 设想与项目目前的技术底座高度一致。通过 [loader_engine.py](file:///c:/green/code_matter/sim_engine/src/loader_engine.py) 实现从 **科研仿真 (Sim)** 到 **科普游戏 (Game)** 的有损但精确的映射，是本项目最具竞争力的创新。

### 实现逻辑 (R-G Converter)
- **输入转卡牌**: 将 Sim 的 `input` 变量及其 `bounds` 映射为玩家手牌。
- **状态转指标**: 将 `state` 变量映射为 UI 上的红条 (HP)、蓝条 (SP) 或隐藏的概率累积器。
- **动力学转引擎**: 将 `dynamics` 公式识别为回合结算逻辑（例如：每回合根据辐射值扣减 HP）。
- **变量简化**: 采用 **归一化/整数化** 处理。将 RR=2.7 映射为“卡牌权重+27%”，将复杂微分方程简化为离散回合步进。

---

## 2. 场景匹配度设计与评估

| 维度 | **场景 A：2型糖尿病管理** | **场景 B：维多利亚时代童工** | **场景 C：1918 全球流感** |
| :--- | :--- | :--- | :--- |
| **Sim 层 (科研)** | 血糖动力学模型 (dt=小时) | 能量代谢系数与伤残率模型 | SIR 疾病传播模型 (dt=天) |
| **Game 层 (科普)** | **[人生抉择]** | **[绝望生存]** | **[公共卫生策略]** |
| **Input -> 卡牌** | `[服药]`、`[大餐]`、`[散步]` | `[14h轮班]`、`[偷窃]`、`[买坏面包]` | `[戴口罩]`、`[去教堂]`、`[居家]` |
| **State -> 指标** | HP (健康), Money (买药) | HP (生命), Money (糊口) | HP, Social (社会资本) |
| **Dynamics -> 结算** | 血糖波动导致并发症卡入堆 | 累计过劳 -> permanent_hp_loss | 暴露率 -> 感染卡出现概率 |
| **评估结论** | **最佳入门场景**：数值稳定，逻辑链清晰。 | **情感冲击力最强**：适合展示“无法通关”哲学。 | **科普价值最高**：展示宏观政策对个体的保护。 |

---

## 3. 设计建议与规则更新 (Docs 建议)

### A. 强化“单包原则” (Already Actioned)
- 严禁在 `mods/stories/` 根目录放置任何独立 YAML。
- 所有的 `Sim` 逻辑和 `Game` 表现层必须共存于子目录下的 `story.yaml`。

### B. 引入“P1-P5 映射标准”
建议在文档中明确 5 种标准动力学识别模式：
1. **P1 (Fixed)**: 恒定扣减（饥饿：每回合 HP-5）。
2. **P2 (Accumulate)**: 负面累积（辐射：每层 +1）。
3. **P3 (Dynamic)**: 随量扣减（由于辐射，每回合 HP - 辐射层数*0.1）。
4. **P4 (Threshold)**: 阈值触发（如果 辐射>50 -> 触发急性病）。
5. **P5 (Probabilistic)**: 概率判定（如果 概率>90% -> 触发）。

### C. “数值遮盖”策略
- 在 Sim 层保留浮点数（如 `0.05`）进行计算。
- 在 Game UI 层将其显示为“概率极低”或“少量影响”，将科研的枯燥转化为直观的视觉反馈。

---

## 4. 结论
现在的系统架构已完全支持该设想。只要坚持**单包封装**和**公式模式化**，您可以非常容易地将任何学术模型快速“游戏化”发布。
