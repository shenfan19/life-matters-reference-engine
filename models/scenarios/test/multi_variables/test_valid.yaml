metadata:
  name: test_valid
  version: 1.0.0
  author: Test User
  description: 一个能够通过验证的测试模型
  date: 2024-10-27
  language: en
  tags:
    - test
    - valid

variables:
  # 输入变量
  temperature:
    type: input
    unit: celsius
    default: 37.0
    description: 体温
  
  heart_rate:
    type: input
    unit: bpm
    default: 70
    description: 心率
  
  # 参数
  base_metabolic_rate:
    type: parameter
    unit: kcal/day
    default: 1500
    description: 基础代谢率
  
  # 状态变量
  energy_level:
    type: state
    unit: kcal
    default: 2000
    description: 能量水平
  
  stress_level:
    type: state
    unit: dimensionless
    default: 0.5
    description: 压力水平

formulas:
  # 能量变化
  energy_level:
    description: 能量水平随时间变化
    dynamics: 
      energy_level: energy_level + (base_metabolic_rate / 24) - (heart_rate * 0.1)
  
  # 压力变化
  stress_level:
    description: 压力水平受体温影响
    dynamics: 
      stress_level: stress_level + ((temperature - 37.0) * 0.01)

simulation:
  dt: 0.1
  t_end: 10.0
  method: euler
  output_variables:
    - energy_level
    - stress_level

optimization:
  enabled: false
